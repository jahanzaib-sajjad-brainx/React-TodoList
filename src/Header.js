import React from "react";

const Header= () => {
    return(
        <header>
            <h1 className="text-black mb-3">To Do List</h1>
           
        </header>
    );
};

export default Header;